<%= form_with(model: tests2) do |form| %>
  <% if tests2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tests2.errors.count, "error") %> prohibited this tests2 from being saved:</h2>

      <ul>
        <% tests2.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
