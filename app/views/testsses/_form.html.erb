<%= form_with(model: testss) do |form| %>
  <% if testss.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(testss.errors.count, "error") %> prohibited this testss from being saved:</h2>

      <ul>
        <% testss.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
